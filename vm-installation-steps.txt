# Instructions to run this project in a VM to train the AI:

sudo apt install git
git clone https://github.com/Martenda/TCC-2.git
# Log-in with github account
cd TCC-2
git checkout -b develop origin/develop
git pull
# Download vscode
sudo apt install Python3.10.11
pip install -r requirements.txt
# Open the project dir on vscode
# Open new Terminal on vscode
# Transfer the Dataset to the VM
# Place the Dataset inside project, on proper dir
# Decompress/unzip the Dataset
